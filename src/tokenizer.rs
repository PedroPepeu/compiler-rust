fn main(input: String) -> String {
    let tokens = [];
    let current = 0;
    while current < input.length() {
        const char: char = input[current].expect("Failed to read {input[current]} char");
    }
    tokens
}
